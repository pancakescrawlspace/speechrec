# Video sources (large binaries)
**/*.mp4
**/*.wmv

# copyrighted content
**/*.srt

# Python virtualenv and bytecode (recreate with: python3 -m venv venv && ./venv/bin/pip install -r requirements.txt)
venv/
venv-*/
__pycache__/

# macOS
.DS_Store

# Engine outputs, review sheets and hand-corrected references (copyrighted text)
work/
references/
